// This is where db configuration will go
// Will be called from initial express server launch to connect to database.
// Haven't decided on SQL or Mongo connection yet.

//This file must be recreated or renamed to config.js to be properly configured (due to .gitignore)
module.exports = {
    db: ''
}